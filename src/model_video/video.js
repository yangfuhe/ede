const model = {
    name: 'video',
    schema: {
        courseId: String,
        sort: Number,
        name: String,
        url: String,
        size: Number,
        createTime: Date,
    }
}

module.exports = model