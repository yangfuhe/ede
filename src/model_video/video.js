const model = {
    name: 'video',
    schema: {
        courseId: String,
        sort: Number,
        name: String,
        url: String,
        size: Number,
        duration: Number,
        createTime: Date,
    }
}

module.exports = model